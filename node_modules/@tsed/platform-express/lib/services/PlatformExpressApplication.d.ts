import { Configuration, PlatformApplication, PlatformHandler } from "@tsed/common";
import Express from "express";
import { PlatformExpressStaticsOptions } from "../interfaces/PlatformExpressStaticsOptions";
import { PlatformExpressRouter } from "./PlatformExpressRouter";
declare global {
    namespace TsED {
        interface Application extends Express.Application {
        }
        interface StaticsOptions extends PlatformExpressStaticsOptions {
        }
    }
}
/**
 * @platform
 * @express
 */
export declare class PlatformExpressApplication extends PlatformExpressRouter implements PlatformApplication<Express.Application, Express.Router> {
    app: Express.Application;
    rawApp: Express.Application;
    rawRouter: Express.Router;
    constructor(platformHandler: PlatformHandler, configuration: Configuration);
    getApp(): Express.Application;
    callback(): Express.Application;
}
