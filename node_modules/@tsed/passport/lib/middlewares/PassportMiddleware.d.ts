import { Context } from "@tsed/common";
import { ProtocolsService } from "../services/ProtocolsService";
export declare class PassportMiddleware {
    protocolsService: ProtocolsService;
    shouldSkip(ctx: Context): any;
    use(ctx: Context): Promise<void>;
    protected catchError(er: any): void;
    private call;
}
