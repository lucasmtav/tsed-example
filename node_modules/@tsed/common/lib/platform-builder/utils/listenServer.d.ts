/// <reference types="node" />
import Http from "http";
import Https from "https";
export declare function listenServer(http: Http.Server | Https.Server, settings: {
    address: string | number;
    port: number;
}): Promise<{
    address: string;
    port: number;
}>;
