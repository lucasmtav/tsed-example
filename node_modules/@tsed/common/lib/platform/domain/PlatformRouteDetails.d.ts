import { EndpointMetadata } from "../../mvc/models/EndpointMetadata";
export declare class PlatformRouteDetails {
    readonly method: string;
    readonly url: string;
    readonly rawBody: boolean;
    readonly endpoint: EndpointMetadata;
    constructor({ endpoint, method, url }: {
        endpoint: EndpointMetadata;
        method: string;
        url: string;
    });
    get name(): string;
    get className(): string;
    get methodClassName(): string;
    get parameters(): import("../..").ParamMetadata[];
    toJSON(): {
        method: string;
        name: string;
        url: string;
        className: string;
        methodClassName: string;
        parameters: import("../..").ParamMetadata[];
        rawBody: boolean;
    };
}
