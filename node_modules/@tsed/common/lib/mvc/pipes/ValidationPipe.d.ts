import { InjectorService } from "@tsed/di";
import { IPipe, ParamMetadata } from "../models/ParamMetadata";
export declare class ValidationPipe implements IPipe {
    private validator;
    constructor(injector: InjectorService);
    coerceTypes(value: any, metadata: ParamMetadata): any;
    skip(value: any, metadata: ParamMetadata): boolean;
    transform(value: any, metadata: ParamMetadata): Promise<any>;
    protected checkIsRequired(value: any, metadata: ParamMetadata): boolean;
}
